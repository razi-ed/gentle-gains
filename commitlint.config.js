// commitlint.config.js
module.exports = {
  extends: ['@commitlint/config-conventional'],
  // Optionally add custom rules here if needed
  // rules: {
  //   'scope-enum': [2, 'always', ['core', 'auth', 'ui', 'docs']],
  // }
};